# План стабилизации и рефакторинга

Дата аудита: 2026-07-27

Статусы:

- `[x] СДЕЛАНО` — пункт выполнен полностью и проверен.
- `[ ] НЕ СДЕЛАНО` — пункт не выполнен либо выполнена только его часть. Частичный результат указан в пояснении.

Этап считается завершённым только тогда, когда выполнены все его пункты. Каждый законченный блок проходит проверки, документируется, фиксируется commit и отправляется в `origin/dev`.

## Этап 1. Навести порядок в базе

- [x] **СДЕЛАНО — сделать чистую локальную копию из `keastayer-cell/bg_foot_project`.**
  Source of truth проверяется в чистом checkout `/private/tmp/bg_foot_project_stage1`, ветка `dev`, remote `origin`.
- [x] **СДЕЛАНО — проверить ветки `dev`/`test`/`main`, CI и текущий deploy-flow.**
  В remote присутствуют `dev`, `test`, `main`, `prod`; `origin/HEAD` указывает на `dev`. Test deploy запускается push в `test`, а `scripts/promote-dev-to-test.sh` выполняет merge `dev -> test`.
- [x] **СДЕЛАНО — исправить рассинхрон Java между Maven и CI.**
  `app/pom.xml`, `mailer/pom.xml` и `.github/workflows/deploy-test.yml` используют Java 21.
- [x] **СДЕЛАНО — убедиться, что backend, frontend и mailer собираются из чистого checkout.**
  Backend и mailer проходят Maven build/test, frontend проходит production build.
- [x] **СДЕЛАНО — зафиксировать README: как запускать, как деплоить, где source of truth.**
  Корневой README стал самодостаточным: описаны требования, PostgreSQL, env-файлы, запуск всех трёх модулей, health endpoints, тесты, ветки, test deploy и отсутствие production workflow.

**Статус этапа 1: ЗАВЕРШЁН.** Все пункты проверены в чистом checkout.

## Этап 2. Защитить критичные сценарии тестами

- [x] **СДЕЛАНО — backend smoke-тест: приложение стартует, `/api/health` отвечает.**
  `ApplicationSmokeTest` поднимает Spring context на изолированной H2 и через MockMvc проверяет статус `UP`.
- [x] **СДЕЛАНО — auth-тесты: login, refresh, logout, reset password, `tokenVersion`.**
  Покрыты login, выдача/rotation/revoke refresh token, создание и завершение password reset, очистка одноразового токена и отклонение устаревших access/refresh sessions.
- [x] **СДЕЛАНО — access-control тесты ролей `SUPER_ADMIN`, `REFEREE`, `TEAM_REP`, `GUEST`.**
  Backend `ApiAccessRuleServiceTest` фиксирует разрешения по роли, HTTP method и URL pattern; frontend guard tests проверяют маршрутные ограничения.
- [x] **СДЕЛАНО — бизнес-тесты заявок, дозаявок, трансферов и турнирной таблицы.**
  Проверены утверждение заявки, синхронизация дозаявки со статусом `APPROVED`, подтверждение трансфера и расчёт таблицы по `VERIFIED` протоколу.
- [x] **СДЕЛАНО — frontend build и минимальные e2e smoke: главная, логин, админка, кабинет представителя.**
  Playwright запускает 4 браузерных smoke-сценария с реальным Vue router/auth flow и изолированным API mock; production build и 55 Vitest тестов проходят.

**Статус этапа 2: ЗАВЕРШЁН.** Backend: 18 тестов; frontend: 55 unit/route + 4 e2e; mailer: 2 теста.

## Этап 3. Разобрать frontend-монолиты

- [x] **СДЕЛАНО — разбить `Admin.vue` на вкладки/секции.**
  Сезоны, команды, игроки, судьи, роли, представители, блокировки, туры и связанные workflow вынесены в компоненты/composables. Заявки и трансферы открываются отдельными маршрутами.
- [x] **СДЕЛАНО — декомпозировать `Tours.vue`.**
  Таблица, шахматка, плей-офф, туры и статистика вынесены в компоненты и тестируемые composables.
- [x] **СДЕЛАНО — декомпозировать `Match.vue`.**
  Страница уменьшена с 1894 до 410 строк. Загрузка и протокол находятся в `useMatchPage`, заявки команд — в `useMatchLineups`, endpoint'ы — в `api/matches`, scoped CSS — в отдельном файле.
- [x] **СДЕЛАНО — декомпозировать `TeamRepDashboard.vue`.**
  Страница уменьшена с 1356 до 382 строк. Dashboard orchestration, форма игрока, входящие трансферы, статусы/форматирование и API разделены на самостоятельные модули.
- [x] **СДЕЛАНО — вынести API-запросы страниц в отдельный `api/` слой.**
  HTTP transport централизован в `api/http.js`; страницы и компоненты используют доменные модули `catalog`, `matches`, `teamRep`, `teamRepTransfers`, `seasonApplications`, `adminSeasons`, `adminLeague` и `auth`. Прямого `fetch` вне transport-слоя нет.
- [x] **СДЕЛАНО — включать глобальный `window.fetch` debug-лог только через `VITE_DEBUG_API_LOGS=true`.**
  Логгер вынесен в `utils/apiDebugLogger.js`, маскирует секреты и покрыт тестами.

**Статус этапа 3: ЗАВЕРШЁН.** Все пункты исходного scope выполнены; frontend проходит 62 unit/route-теста, production build и 5 Playwright smoke-сценариев.

## Этап 4. Укрепить backend

- [x] **СДЕЛАНО — проверить границы сервисов: тонкие контроллеры, бизнес-логика в сервисах.**
  Управление игроками и судьями вынесено в сервисы; сезонные query-проекции больше не собираются через репозитории в контроллере.
- [x] **СДЕЛАНО — пройти транзакции: `readOnly`, запись, блокировки и идемпотентность.**
  Чтение/запись разделены; refresh rotation, модерация заявок и решения по трансферам защищены `PESSIMISTIC_WRITE` и проверкой статуса внутри транзакции.
- [x] **СДЕЛАНО — унифицировать ошибки и DTO, проверить validation annotations.**
  MVC и security возвращают единый `ApiErrorResponse`; добавлены field errors, безопасный 500 и ограничения входных DTO.
- [x] **СДЕЛАНО — упростить и документировать access rules endpoint'ов.**
  Устранена разница доступа к публичному GET с токеном и без него; слои и ролевая матрица описаны в `docs/Рефакторинг 28.07.2026/backend-access-rules.md`.
- [x] **СДЕЛАНО — проверить N+1 на матчах, таблицах, командах и игроках.**
  Подтверждены fetch-query матчей/таблиц; команды, игроки, заявки, составы, судьи и трансферы переведены на пакетную загрузку связей, фото и счётчиков.

**Статус этапа 4: ЗАВЕРШЁН.** Backend проходит 23 теста; подробный отчёт и принятые решения находятся в `docs/Рефакторинг 28.07.2026/refactoring-stage-4-backend.md`.

## Этап 5. Mailer и уведомления

- [x] **СДЕЛАНО — проверить retry, lock, stale lock и дедупликацию очереди.**
  Исправлен возврат зависших `PROCESSING`, добавлены lock ownership, exponential backoff, атомарные логи и deduplication keys.
- [x] **СДЕЛАНО — добавить health/readiness для mailer.**
  Readiness включает datasource и отдельный `mailerQueue` indicator со статистикой очереди.
- [x] **СДЕЛАНО — покрыть шаблоны тестами рендера.**
  Проверены макросы, незаполненные значения и HTML escaping пользовательских данных.
- [x] **СДЕЛАНО — сделать понятный режим `log` для test и `smtp` для production.**
  Добавлены профили `test`/`prod`, fail-fast проверка SMTP credentials и безопасное логирование без тела письма.

**Статус этапа 5: ЗАВЕРШЁН.** Подробности находятся в `docs/Рефакторинг 28.07.2026/refactoring-stage-5-mailer.md`.

## Этап 6. CI/CD и эксплуатация

- [x] **СДЕЛАНО — CI на PR: backend tests, frontend build, lint, migration validation.**
  CI проверяет backend, mailer, ESLint/Vitest/build frontend, append-only Flyway history и применение миграций на PostgreSQL 16.
- [x] **СДЕЛАНО — deploy test: сборка, миграции, health-check, rollback-подсказки.**
  В release входят три модуля; перед deploy создаётся DB/runtime backup, а при провале автоматически возвращаются jars/frontend.
- [x] **СДЕЛАНО — логи: journald/logrotate, без секретов.**
  Зафиксированы лимиты/retention journald, rate limits сервисов, Nginx logrotate и тихие `test`/`prod` профили.
- [x] **СДЕЛАНО — backup/restore БД test/prod.**
  Добавлены защищённые backup/restore scripts, checksum, retention и ежедневные systemd timers.
- [x] **СДЕЛАНО — smoke-check после deploy отдельным скриптом.**
  Переиспользуемый скрипт проверяет backend, frontend и readiness mailer локально и из GitHub Actions.

**Статус этапа 6: ЗАВЕРШЁН.** Полный runbook находится в `docs/operations.md`, отчёт — в `docs/Рефакторинг 28.07.2026/refactoring-stage-6-operations.md`.

## Этап 7. Продуктовая полировка

- [ ] **НЕ СДЕЛАНО — пройти пути болельщика, представителя команды, рефери и супер-админа.**
- [x] **СДЕЛАНО — упростить навигацию в админке.**
  На desktop разделы собраны в компактную липкую боковую навигацию, на mobile
  заменены одним нативным меню без горизонтального переполнения. Инструменты
  разделены на группы «Турнир», «Участники» и «Доступ», переходы на отдельные
  страницы отмечены отдельно. Ролевой аудит устранил расхождение для `REFEREE`:
  рефери видит разрешённый backend-раздел «Лига», но не видит роли,
  представителей и блокировки. Поведение проверено unit-тестами и 30
  Playwright smoke-сценариями на desktop, Android и iPhone/WebKit.
- [ ] **НЕ СДЕЛАНО — улучшить пустые состояния, ошибки и подтверждения опасных действий.**
  Локальные улучшения есть, системный проход по приложению не выполнен.
- [x] **СДЕЛАНО — привести API Explorer к актуальному backend API.**
  Каталог синхронизирован со всеми 97 mapping'ами backend. У каждого endpoint есть экран теста; добавлены поиск, фильтры, адаптивная раскладка, подтверждение изменяющих запросов и скачивание бинарных ответов. Компоновка проверяется отдельным E2E-набором на desktop Chromium, двух Android и двух iPhone/WebKit.

**Статус этапа 7: В РАБОТЕ.** API Explorer и навигация админки завершены;
открыты ролевые пользовательские пути и системная полировка пустых состояний,
ошибок и подтверждений.

## Фактический порядок дальнейшей работы

1. Выполнить этап 7.
